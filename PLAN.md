# PLAN.md — 논문 요약 · Q&A MVP

> PRD.md 기준 PDCA Plan 단계 산출물. Feature: `paper-ai-mvp`

## 이번 사이클 목표

PRD.md에 정의된 두 가지 필수 기능(논문 요약, 논문 Q&A 챗봇)을 Next.js + OpenAI API로 구현하여, 로그인 없이 PDF 업로드 → AI 요약 확인 → 채팅 질의응답까지 이어지는 흐름이 실제로 동작하는 MVP를 완성한다.

## 성공 기준 (Definition of Done)

- [ ] PDF 업로드(최대 30페이지) → 텍스트 추출 → 500자 이내 핵심 요약 + 4단계 상세 요약이 화면에 표시된다.
- [ ] 텍스트 추출 불가 PDF는 "텍스트 추출이 불가능한 파일입니다" 메시지로 안내되고 처리가 중단된다.
- [ ] 채팅으로 질문 시, 업로드된 논문 내용에 근거한 답변과 관련 문단·페이지 번호가 함께 표시된다.
- [ ] 논문과 무관한 질문에는 "논문과 관련된 질문만 답변할 수 있습니다"로 응답한다.
- [ ] AI API 오류/네트워크 오류 시 "일시적인 오류가 발생했습니다. 다시 시도해주세요"로 안내한다.
- [ ] 업로드 → 요약 → Q&A 전체 흐름을 처음부터 끝까지 직접 실행해 확인한다.
- [ ] 브라우저를 껐다 켜도 96시간 이내면 이전 논문·요약·대화가 그대로 보인다.

## 작업 순서 (선행 작업부터)

1. Next.js 프로젝트 기본 세팅 — 로그인 없이 바로 진입하는 기본 화면 구성
2. 논문 PDF 업로드 UI 및 파일 업로드 처리 기능 구현
3. 업로드된 PDF에서 텍스트 추출(파싱) 기능 구현
4. 추출한 텍스트를 OpenAI API로 전달해 500자 이내 핵심 요약을 생성하는 기능 구현
5. 4단계 상세 요약(Key Terms & Definitions / Motivation & Problem Statement / Method & Key Results / Conclusion & Impact) 생성 기능 구현
6. 기술 용어는 영어 원문 유지, 문장은 자연스러운 한국어로 작성하는 규칙을 요약과 Q&A 답변 모두에 적용
7. 요약 결과(핵심 요약 + 4단계 상세 요약)를 보여주는 화면 UI 구현
8. 논문 내용 기반 채팅 Q&A 인터페이스(UI) 구현
9. 채팅 답변 로직 구현 — ① 논문에 근거가 있으면 근거와 함께 답변, ② 논문에 등장하나 설명이 없는 개념은 일반 지식으로 답하고 "이 내용은 논문에 없는 일반 지식입니다" 표시, ③ 논문과 무관한 질문은 "논문과 관련된 질문만 답변할 수 있습니다"로 안내
10. Q&A 답변에 관련 문단·페이지 번호를 함께 표시하는 기능 구현
11. 텍스트 추출 실패, AI API 오류 등 예외 상황에 대한 기본 에러 처리 구현
12. 업로드 → 요약 → Q&A 전체 흐름 통합 테스트
13. 브라우저 식별용 세션 쿠키 발급 및 논문·요약·대화 저장 기능 구현
14. 96시간 이내 재접속 시 이전 논문·요약·대화를 이어서 보여주는 기능 구현
15. 96시간 경과 데이터 자동 삭제 작업 설정
