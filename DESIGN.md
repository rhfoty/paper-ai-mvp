# DESIGN.md — 논문 요약 · Q&A MVP 설계

> PLAN.md 기준 PDCA Design 단계 산출물. Feature: `paper-ai-mvp`
> 구조 방식: **적당히 정리(Option C)** — 화면/API는 기능별(업로드·요약·채팅)로 나누고, PDF 읽기·AI 호출 같은 공통 로직만 별도 함수로 분리. 과하게 쪼개지 않아 지금 규모(기능 2개)에 딱 맞음.

---

## 1. 화면 구성 (무엇이 어디에 보이는지)

데스크탑 화면 기준, 한 페이지 안에서 상태에 따라 내용이 바뀌는 구조입니다.

### 화면 A — 업로드 화면 (처음 접속했을 때)

```
┌─────────────────────────────────────────────┐
│  [로고/제목] 논문 리딩 어시스턴트              │
│                                               │
│         ┌───────────────────────┐           │
│         │   PDF 파일을 여기로     │           │
│         │   끌어다 놓거나 클릭     │           │
│         │   (PDF만, 최대 30페이지) │           │
│         └───────────────────────┘           │
│                                               │
│  ※ 업로드한 논문은 96시간 후 자동 삭제됩니다   │
└─────────────────────────────────────────────┘
```
- 화면 가운데에 업로드 영역만 크게 보여줍니다. (다른 기능은 아직 안 보임)

### 화면 B — 분석 중 화면 (업로드 직후)

- 업로드 영역 자리에 "논문을 분석하고 있습니다..." 로딩 표시만 보여줍니다.
- 텍스트 추출 실패 시 이 화면에서 바로 "텍스트 추출이 불가능한 파일입니다" 안내로 전환됩니다.

### 화면 C — 결과 화면 (분석 완료 후, 메인 작업 화면)

```
┌─────────────────────────────────────────────┐
│ [새 논문 업로드]                              │
├───────────────────────────┬───────────────────┤
│ 핵심 요약 (500자 이내)       │                   │
│ ─────────────────────      │   Q&A 채팅         │
│ [상세 요약 탭]               │   ─────────       │
│  · Key Terms & Definitions │   질문 입력창       │
│  · Motivation & Problem    │   답변 + (p.3 참고)  │
│  · Method & Key Results    │   답변 + (p.7 참고)  │
│  · Conclusion & Impact     │                   │
│                             │   [질문 입력]       │
│ (왼쪽, 약 60% 너비)          │ (오른쪽, 약 40% 너비)│
└───────────────────────────┴───────────────────┘
```
- **왼쪽**: 핵심 요약이 최상단에 고정되고, 그 아래 4단계 상세 요약이 이어서 보입니다.
- **오른쪽**: 채팅 Q&A 패널이 항상 함께 보여서, 요약을 읽으며 바로 질문할 수 있습니다.
- 상단 "새 논문 업로드" 버튼을 누르면 화면 A로 돌아가고, 이전 논문 내용은 대체됩니다.
- 모든 에러(요약 실패, 답변 실패)는 해당 위치에 "일시적인 오류가 발생했습니다. 다시 시도해주세요" 문구로 표시됩니다.

---

## 2. 데이터 흐름 (입력 → 처리 → 출력)

### 흐름 1) 논문 업로드 → 요약

```
[입력]                  [처리 (서버)]                          [출력]
PDF 파일 업로드   →   ① 페이지 수·형식 확인 (PDF, ≤20p)   →
                      ② PDF에서 텍스트 추출               →
                         (페이지 단위로 나눠 [1페이지 본문][2페이지 본문]…
                          형태로 페이지 번호를 붙여 보관 — 이후 답변 근거
                          페이지 표시에 사용, 번호를 AI가 추측하지 않게 함)
                         (실패 시 → "텍스트 추출 불가" 메시지로 종료)
                      ③ 추출한 텍스트를 세션에 저장         →
                      ④ OpenAI API로 500자 핵심 요약 요청   →
                      ⑤ OpenAI API로 4단계 상세 요약 요청   →   화면에 핵심요약 +
                         (실패 시 → "일시적 오류" 메시지)         4단계 상세요약 표시
```

### 흐름 2) 채팅 질문 → 답변

```
[입력]                  [처리 (서버)]                          [출력]
사용자 질문 입력   →   ① 현재 세션에 저장된 논문 텍스트 확인   →
                      ② 질문 + (페이지 번호가 붙은) 논문 텍스트를        →
                         OpenAI API에 전달 (답변의 페이지 번호는 이 표시를
                         그대로 인용하게 하고, 번호를 새로 지어내지 않게 함)
                      ③ 논문 근거 여부 판단                  →
                         - 논문 안에 근거 있음 → 답변 + 관련 문단/페이지
                         - 논문에 없는 일반 지식 → 답변 + "일반 지식입니다" 표시
                         - 논문과 무관 → "논문과 관련된 질문만..." 안내
                         (API 오류 시 → "일시적 오류" 메시지)      →   채팅창에 답변 표시
```

### 세션 유지 흐름 (96시간)

```
브라우저 최초 접속 → 세션 식별값(쿠키) 발급 →
업로드한 논문·요약·대화가 이 식별값과 함께 저장 →
96시간 이내 같은 브라우저 재접속 시 → 저장된 내용 그대로 이어서 표시 →
96시간 경과 → 자동 삭제
```

---

## 공통 AI 규칙 (요약·Q&A 답변 모두 적용)

- **환각 금지**: AI에게 보내는 지시문에 "제공된 논문 텍스트에 없는 내용은 절대 추가하지 말 것" 규칙을 항상 포함한다. 요약에도, 채팅 답변에도 동일하게 적용한다.
- **영어 용어 유지**: 핵심 기술 용어·지표·약어·포맷 명칭은 한국어로 번역하지 않고 영어 원문 그대로 사용하고, 문장 구조만 자연스러운 한국어로 작성한다. 이 규칙도 요약과 채팅 답변 모두에 적용한다.

---

## 3. 기술 선택

### 기본 (이미 확정)

| 기술 | 역할 |
|------|------|
| **Next.js** | 화면(프론트) + 서버 처리(API)를 한 프로젝트에서 담당. 최종적으로 Vercel에 그대로 배포. |

### 추가로 필요한 기술 (쉬운 설명 포함)

| 기술 | 왜 필요한가 (쉬운 설명 1줄) |
|------|------------------------------|
| **OpenAI API (openai 패키지)** | 요약 문장과 채팅 답변을 실제로 만들어주는 AI에게 요청을 보내고 결과를 받아오는 연결 도구입니다. |
| **PDF 텍스트 추출 라이브러리** (예: pdf-parse) | PDF 파일 안의 글자를 컴퓨터가 읽을 수 있는 순수 텍스트로 꺼내주는 도구입니다. (이미지로 스캔된 PDF는 이 단계에서 텍스트가 안 나와 "추출 불가"로 처리됩니다) |
| **Supabase** (`.env`에 이미 토큰 있음) | 업로드한 논문 내용·요약·대화 기록을 저장해 두는 창고입니다. 로그인 없이도 96시간 동안 같은 브라우저로 다시 접속했을 때 이전 내용을 보여주려면 어딘가에 저장해 둬야 하는데, 그 저장소 역할입니다. |
| **세션 쿠키** (Next.js 내장 기능만 사용) | "이 브라우저가 어떤 논문을 올렸는지" 로그인 없이 구별하기 위해 브라우저에 남겨두는 작은 식별표입니다. |
| **정기 삭제 작업** (Vercel Cron Jobs) | 정해진 시간마다 자동으로 한 번씩 실행되어, 96시간이 지난 오래된 논문·대화 기록을 자동으로 지워주는 예약 청소부입니다. |

---

## 4. 다음 단계

1. [ ] 위 화면 구성·데이터 흐름·기술 선택에 이견이 없으면 구현(Do 단계) 시작
2. [ ] PLAN.md의 작업 순서 1번부터 순서대로 구현
