# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 현황

이 저장소는 아직 코드가 작성되지 않은 **기획 단계**입니다 (`package.json`, 소스 디렉토리 없음). 존재하는 파일은 `PRD.md`(상세 기획서), `prd_lite.md`(요약 기획서), 그리고 bkit 플러그인의 PDCA 상태 파일(`.bkit/`)뿐입니다. `.bkit/state/pdca-status.json` 기준 파이프라인은 `Phase 1 (schema)`, 레벨 `Dynamic` 단계에 있습니다.

**따라서 빌드/린트/테스트 명령은 아직 존재하지 않습니다.** Next.js 프로젝트가 실제로 스캐폴딩되면 이 섹션을 `npm run dev` / `npm run build` / `npm run lint` 등 실제 명령으로 갱신해야 합니다.

## 한 줄 요약

논문(PDF)을 업로드하면 AI가 구조화된 요약을 제공하고, 업로드한 논문 내용을 근거로 채팅 Q&A를 할 수 있는 웹앱. 대상 사용자는 논문을 자주 읽는 대학원생(특히 생명공학·의약학 전공)이다.

## 기술 스택 (예정)

- **Next.js** — 프론트엔드와 API 라우트를 한 프로젝트에서 처리. Vercel 배포를 전제로 고정된 선택.
- **OpenAI API** — 논문 요약 및 Q&A 응답 생성.
- 인증/로그인 없음 — 브라우저 세션 기반으로만 논문·대화를 유지.

## 핵심 기능 규칙 (구현 시 반드시 지킬 것)

기능은 두 가지뿐이며, 각 기능에는 PRD.md에 명시된 엄격한 AI 동작 규칙이 있다. 구현 시 아래 규칙이 코드/프롬프트에 실제로 반영되어야 한다.

### 1. 논문 요약
- 처리 대상은 PDF만, 최대 30페이지.
- 텍스트 추출 불가(스캔 이미지 등) 시 "텍스트 추출이 불가능한 파일입니다" 메시지를 보여주고 중단.
- 논문 원문에 없는 내용을 추측해 넣지 않는다 (환각 금지).
- 맨 처음 제공하는 핵심 요약은 500자 이내. 이어지는 4단계 상세 요약(Key Terms & Definitions / Motivation & Problem Statement / Method & Key Results / Conclusion & Impact)에는 글자 수 제한 없음.
- 핵심 기술 용어·지표·약어·포맷 명칭은 한국어로 번역하지 않고 영어 원문 그대로 사용, 문장 구조만 자연스러운 한국어로 작성 (Technical Terms in English, Natural Korean Phrasing).
- AI API 오류/네트워크 문제 시 "일시적인 오류가 발생했습니다. 다시 시도해주세요" 안내.

### 2. 논문 Q&A 챗봇
- 한 세션에는 논문 한 편만 다루며, 새 논문 업로드 시 이전 논문 내용은 대체된다.
- 답변은 기본적으로 업로드된 논문 내용에 근거해야 한다.
- 논문에 등장하지만 논문 자체에는 자세히 설명되지 않은 개념을 질문하면 일반 지식으로 설명하되 "이 내용은 논문에 없는 일반 지식입니다"라고 출처를 표시.
- 논문과 무관한 질문에는 "논문과 관련된 질문만 답변할 수 있습니다"라고 안내.
- 답변 시 관련 문단·페이지 번호를 함께 표시.
- 용어 처리 규칙, 오류 메시지 규칙은 요약 기능과 동일.

## 비범위 (만들지 않는 기능)

다음은 명시적으로 이번 범위에서 제외된다. 관련 요청이 있어도 이 파일들 안에서 새 기능을 임의로 확장하지 말 것:
- 여러 논문 비교·분석 기능
- 논문 검색/추천 기능 (외부 DB 연동 포함)
- 회원 간 협업(공유, 코멘트) 기능
- 논문 자동 번역 기능
- 로그인/회원가입 (인증 없이 바로 이용)
- OCR(이미지 텍스트 인식)

## 보안 및 데이터 처리

- 외부 API 키(OpenAI 등)는 `.env`에 저장하고 절대 코드나 커밋에 포함하지 않는다. `.env`는 이미 `.gitignore`에 등록되어 있다.
- 업로드된 논문 파일과 요약·대화 내용은 업로드 후 96시간이 지나면 자동 삭제되어야 한다. 로그인 없이도 96시간 이내에는 같은 브라우저로 재접속 시 이전 논문·대화를 이어볼 수 있어야 한다 (세션/쿠키 기반 식별 필요).
- 업로드된 논문은 비공개로 취급하며 제3자와 공유하지 않는다.

## 디자인 톤 (구현 시 참고)

- 깔끔하고 신뢰감 있는 학술적 미니멀 느낌.
- 메인 색상은 차분한 블루 계열.
- 데스크탑 우선 레이아웃 (논문 PDF를 보며 작업), 모바일 대응은 이후 과제.

## bkit 워크플로우

이 프로젝트는 bkit 플러그인의 PDCA(Plan-Design-Do-Check-Act) 방식으로 진행 중이다. 새 기능 작업 시 `.bkit/state/pdca-status.json`의 현재 phase를 확인하고, 임의로 다음 단계(설계/구현)를 건너뛰지 말 것.

## 작업 규칙 (Claude Code 준수 사항)

- 모든 설명과 주석은 한국어로 작성한다.
- 새 파일은 `my-app` 폴더 안에만 만든다. 상위 폴더나 다른 위치에 파일을 생성하지 않는다.
- 기술 스택은 PRD에 정한 대로 **Next.js로 고정**한다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다. 배포는 **Vercel**을 사용한다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- `.env` 등 비밀 정보가 담긴 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase가 필요하면 Supabase CLI를 설치하고 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치하고 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.
- 파일을 지워야 할 때는 바로 삭제하지 말고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 실제 삭제는 사용자가 직접 확인 후 진행한다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.

## 작업 절차 (검증 루프)

코드를 변경할 때는 아래 루프를 통과할 때까지 반복한다.

1. **변경한다** — 코드를 수정한다.
2. **결과를 직접 확인한다** — 브라우저로 열기/실행 등 실제 동작을 확인한다. 확인 없이 완료로 보고하지 않는다.
3. **스스로 코드 리뷰한다** — 변경 내용을 다시 검토한다.
4. **문제가 있으면 고치고 다시 1)로 돌아간다.**
5. **통과하면** 무엇을 왜 바꿨는지 한 줄로 요약해 알린다.

## 기술 트러블슈팅 노트 (반복 방지)

- `pdf-parse`처럼 `pdfjs-dist` 기반이라 워커 파일을 쓰는 라이브러리를 서버 코드(API 라우트)에서 쓸 때는 `next.config.ts`의 `serverExternalPackages`에 등록한다. 등록하지 않으면 번들링 과정에서 워커 파일 경로를 못 찾아 "Setting up fake worker failed" 오류가 난다.
- 브라우저로 직접 실행 확인할 때는 dev 서버와 같은 origin인 `localhost`로 접속한다. `127.0.0.1`로 접속하면 정적 청크가 403으로 막혀 화면이 있는데도 안 되는 것처럼 보일 수 있다.
- 다른 폴더의 `node_modules`를 그대로 옮겨써야 할 때는 `cp -r` 대신 `npm install`로 새로 설치한다. `cp`는 `node_modules/.bin`의 실행 파일 연결이 깨져 `next: command not found` 같은 오류를 일으킬 수 있다.
